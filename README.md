# Automa-o-pyautogui
automação de cadastro e dados de uma planilha para sistemas.
print('ola mundo') # Print executa a ação desejada
nome = 'augusto' # O nome = significa uma variavel ele atrela ou da um significado a uma outra palavra
cor_favorita = 'azul' # A variavel define textos em uma categoria sendo assim a cor_favorita é azul
print(nome, cor_favorita) # Print para ver o resultado desta ação
